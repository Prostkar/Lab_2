#include <stdio.h>
#include <math.h>

int main() {
    int a, b;
    double x;

    printf("Enter a and b:\n");
    scanf_s("%d %d", &a, &b);

    x = (2 * cos(a) - pow(b, 2)) / (3 * (pow(a, 2) - 3 * b));

    printf("Result x = %.4lf\n", x);

    return 0;
}
